# Mini Plane 前端 —— pnpm 安装配置
#
# ── 为什么必须有这个文件（2026-09-18 实测）────────────────────────
# Windows 上 pnpm 默认的 isolated 链接模式会失败：
#
#   [UNKNOWN] UNKNOWN: unknown error, symlink
#     '..\std-env@4.2.0\node_modules\std-env'
#     -> 'C:\palne\frontend\node_modules\.pnpm\node_modules\std-env'
#
# 根因不是 pnpm 的 bug，而是 Windows 默认不允许非管理员进程创建符号链接
# （同类报错在别处会写成 `os error 1314: The client does not have a required privilege`）。
# 失败点在 hoist 阶段往 node_modules/.pnpm/node_modules/ 建链接，于是整笔安装回滚
# —— 表现是"装不上任何新依赖"，而 package.json / lockfile 保持原样（事务回滚干净）。
#
# 社区共识有两条路：开 Windows 开发者模式（改机器配置，用户侧动作），或改 pnpm 的取包方式。
# 这里选后者，因为它对**所有在这台机器上开发的人**都成立，且随仓库走：
#
#   package-import-method=copy  → 从 pnpm store 取包时用复制而不是硬/软链接，
#                                 绕开 NTFS 上的链接权限问题。**本次实测生效的就是这一条**
#                                 （加之前在同一个包上失败，加之后 `pnpm add` 成功）。
#   node-linker=hoisted         → 用 npm/yarn 那种扁平 node_modules。本次**没有真正切换**
#                                 （增量安装保留了既有 isolated 布局），保留它作为
#                                 "重装时一并生效"的保险：真要做全量重装的话，
#                                 两条一起才能保证下次也不会踩同一个坑。
#
# 影响面（**这是项目级决策，不是随手一改**）：
# - `pnpm-lock.yaml` 记录依赖图，与链接模式无关，因此 `--frozen-lockfile` 照常；
# - CI（.github/workflows/ci.yml 的 frontend job）与 frontend/Dockerfile 都执行
#   `pnpm install --frozen-lockfile`，会**自动继承本文件**，不需要各自加参数；
# - 代价：`package-import-method=copy` 让 node_modules 变大、安装稍慢（复制而非链接）。
#   换来的是 Windows 上能装新依赖 —— 这一条是 E2E 测试能落地的前提。

package-import-method=copy
node-linker=hoisted
