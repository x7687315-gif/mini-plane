# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.ruff_cache/
.pytest_cache/

# Django / 环境变量（敏感，禁止入库）
# .env 同时覆盖 backend/.env 与仓库根 .env（compose 变量插值用），两者都不入库
.env
db.sqlite3
staticfiles/
media/

# Sprint 6：无 Docker 的本地 Celery 通道（filesystem broker 的落盘队列）
.celery/
control/
celerybeat-schedule*

# 本机工具状态（安全扫描 / 工作记忆），运行产物不入库
.mimosa/
.workbuddy/

# IDE & OS
.idea/
.vscode/
.DS_Store
Thumbs.db
