"""测试与冒烟脚本的凭据工具（Sprint 8 收尾统一）。

测试密码不写进源码：一来是仓库卫生（Mimosa 扫描按"硬编码凭据"拦截，
真实项目里也是规矩——源码里出现可登录的密码字面量迟早出事）；
二来测试从不依赖密码的具体值，只要求它能通过 Django 密码校验器
（≥8 位、非常见、不全数字）。

按进程随机生成，同一次测试运行内保持一致；每次重跑换新值，无跨运行状态。
"""

from uuid import uuid4

TEST_PASSWORD = f"Pw-{uuid4().hex}-A1"
