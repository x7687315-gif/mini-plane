-r base.txt
# Sprint 8：仅容器/生产环境使用的依赖（本地开发不需要装）
# gunicorn：计划基线写的是 23.x，按 §1.1"以安装时最新稳定为准"上调至当前最新稳定线
gunicorn==26.2.0
# whitenoise：容器内 DEBUG=False 时 Django 不再自己服务静态文件，
# 用它从 collectstatic 的产物直接服务 admin / DRF browsable API（只在 container 配置里启用）
whitenoise==6.12.0
